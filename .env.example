# jobbot3000 Environment Configuration
# Copy this file to .env or .env.local and fill in your API tokens

# =============================================================================
# ATS Provider API Tokens (Required for live job scraping)
# =============================================================================
# These tokens are optional in development when using mocks (see feature flags below)
# but required for production use.

# Greenhouse API Token
# Get your token at: https://developers.greenhouse.io/harvest.html#authentication
# Navigate to: Your Greenhouse account  Configure  Dev Center  API Credential Management
JOBBOT_GREENHOUSE_TOKEN=

# Lever API Token
# Get your token at: https://hire.lever.co/developer/documentation#authentication
# Navigate to: Settings  Integrations  API
JOBBOT_LEVER_API_TOKEN=

# SmartRecruiters OAuth Token
# Get your token at: https://developers.smartrecruiters.com/docs/getting-started
# Navigate to: Settings  API & Integrations  API Credentials
JOBBOT_SMARTRECRUITERS_TOKEN=

# Workable API Token
# Get your token at: https://workable.readme.io/reference/generate-an-access-token
# Navigate to: Settings  Integrations  API
JOBBOT_WORKABLE_TOKEN=

# =============================================================================
# Web Server Configuration (Optional)
# =============================================================================

# Environment: development | staging | production
# JOBBOT_WEB_ENV=development

# Server host (default: 127.0.0.1 in development, 0.0.0.0 in production)
# JOBBOT_WEB_HOST=127.0.0.1

# Server port (default: 3100 in development)
# JOBBOT_WEB_PORT=3100

# CSRF token (auto-generated if not set)
# JOBBOT_WEB_CSRF_TOKEN=

# =============================================================================
# Feature Flags (Optional)
# =============================================================================

# Use mock data instead of real API calls (useful for development without API tokens)
# Set to 'true' to enable mock mode and bypass the need for API tokens
JOBBOT_FEATURE_SCRAPING_MOCKS=true

# Enable/disable weekly summary notifications
# JOBBOT_FEATURE_NOTIFICATIONS_WEEKLY=true

# =============================================================================
# HTTP Client Configuration (Optional)
# =============================================================================

# Maximum retry attempts for failed HTTP requests (0-5, default: 2)
# JOBBOT_HTTP_MAX_RETRIES=2

# Base backoff delay in milliseconds (default: 250)
# JOBBOT_HTTP_BACKOFF_MS=250

# Circuit breaker threshold - trips after N consecutive failures (default: 5)
# JOBBOT_HTTP_CIRCUIT_BREAKER_THRESHOLD=5

# Circuit breaker reset window in milliseconds (default: 30000)
# JOBBOT_HTTP_CIRCUIT_BREAKER_RESET_MS=30000

# =============================================================================
# Rate Limiting (Optional)
# =============================================================================

# Rate limit window in milliseconds (default: 60000)
# JOBBOT_WEB_RATE_LIMIT_WINDOW_MS=60000

# Maximum requests per window (default: 30 in development, 15 in production)
# JOBBOT_WEB_RATE_LIMIT_MAX=30

# =============================================================================
# Audit Logging (Optional)
# =============================================================================

# Path to audit log file (default: data/audit/audit-log.jsonl)
# JOBBOT_AUDIT_LOG=data/audit/audit-log.jsonl

# Number of days to retain audit logs (default: 30)
# JOBBOT_AUDIT_RETENTION_DAYS=30
